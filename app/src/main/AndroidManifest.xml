<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.prakash.androidcodeshop.myknowledgebase">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.AndroidTOCActivity" />
        <activity
            android:name=".activities.CodeDisplayDialogActivity"
            android:exported="true"
            android:label="@string/title_activity_code_display_dialog"
            android:theme="@style/DialogActivity" />
        <activity android:name=".activities.ContentDisplayCH1Activity" />
        <activity android:name=".activities.ContentDisplayCH2Activity"/>
    </application>

</manifest>